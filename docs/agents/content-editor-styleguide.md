# Content_Editor Style Guide

**Status:** Starter — empty by design. Grows from real drafts, either logged by `Content_Editor` itself or written directly by a human reviewer (Mihal). Both are treated as binding guidance with equal weight - a human note here isn't a "suggestion," it's a rule the next edit pass must follow.

## How this file is meant to grow

Every time `Content_Editor` catches a real cliché, tic, or readability problem in a `Content_Master` draft, add it here as a rule with a short example — so the next review (by this role or a human) applies the same standard instead of relying on memory. Prefer "avoid X, do Y" pairs over abstract advice.

## 1. Clichés to cut

- **"У [чего-то] есть обратная сторона медали"** (the "coin has another side" metaphor). Cut the coin/medal image entirely and state the trade-off directly.
  - Avoid: "Но у этой медали есть обратная сторона, и авторы не стали её прятать."
  - Do: "Но у этого эффекта есть цена, и авторы её не скрывают."
- **"Лекарство от одной болезни рискует вызвать другую"** (the "cure one disease, cause another" idiom, incl. variants like "одно лечит — другое калечит"). Say the trade-off plainly instead of reaching for the medical metaphor.
  - Avoid: "лекарство от одной болезни рискует вызвать другую"
  - Do: "решение одной проблемы здесь оборачивается другой"
- **"Охота за [абстрактным существительным]"** ("hunt for X") as a stock closing flourish for research summaries — mildly trite, prefer a plain verb.
  - Avoid: "продолжит охоту за истинной природой самых первых звёзд"
  - Do: "продолжит искать истинную природу самых первых звёзд"
- **"У удачи/успеха есть заноза/шип"** — same family as the "coin/medal" cliché above: a physical-object metaphor standing in for "but there's a catch." Cut the object image, state the caveat directly.
  - Avoid: "у этой удачи есть заноза: мы до сих пор не знаем..."
  - Do: "но есть загвоздка: мы до сих пор не знаем..."

## 2. Style rules

- **Proofread for stray Latin characters inside Cyrillic words.** Earlier pipeline stages (translation/drafting) sometimes leave mixed-script typos or untranslated English words embedded mid-sentence — these are easy to miss on a normal read because the word "looks" right at a glance. Check character-by-character, not just for meaning.
  - Examples caught: "univеrсальность" (Latin "univ" + Cyrillic remainder), "рождать more тяжёлых звёзд" (bare English word "more" left in place of "больше").
- **Translation calques to watch for and localize:**
  - "детекции" (from EN "detections") → "находки" / "наблюдения".
  - "температурный пол" (from EN "temperature floor") → "минимальная температура" / "нижний порог температуры".
  - "срезать углы" (from EN "cut corners") → prefer native phrasing like "отказаться от полумер" / "не идти на компромиссы" depending on context (note: the idiom can mean either "worked sloppily" or, in negated form, "was thorough" — translate the intended sense, don't just swap words).
  - "полу-аналитический" hyphenated — in Russian the "полу-" prefix normally fuses without a hyphen: "полуаналитический".
- **Don't stack "просто/попросту" in adjacent clauses of the same sentence or paragraph** — reads as a verbal tic once you notice it. Vary the word (e.g. "хотя бы потому, что…", "увеличенное число…") instead of reusing "просто" twice in quick succession.

## 3. Per-product tone notes

Voice varies by product (see each product's own canon/style references) — this section is for editing rules that only apply to one product, not the org-wide rules above.

- **NebulaCast**: *(none logged yet)*
- **Roads of Times**: *(none logged yet)*
- **Stellar Attractor**: *(none logged yet)*
- **Visualization Studio**: *(none logged yet)*
